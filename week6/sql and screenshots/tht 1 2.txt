cd wop-assignments
cd week1-node

echo "console.log('Hello world! 0 (I accidentally deleted the first Hello World) because I used >');" >> my-script.js
echo "var _ = require('lodash'); console.log(_.camelCase('Foo Bar'));" >> my-script.js

nodemon my-script.js

npm set init-scripts "nodemon my-script.js"

git pull origin master

cd -

git push -u origin master

echo "const express = require('express')
const app = express()
const port = 3000

app.get('/', (req, res) => {
  res.send('Hello World!')
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})" >> app.js


echo "'use strict';" >> app.js

echo "app.use(express.static('public'));" >> app.js

echo "app.get('/catinfo', (req, res) => {
  const cat = {
    name: 'Frank',
    birthdate: '2010-12-25',
    weight: 5,
  };
  res.json(cat);
});" >> app.js

git commit -m "hello"

git push origin master

console.log(`server run at http://localhost:3000`)

node app.js

git checkout -b server-two

git push origin server-two

git pull origin server-two

git checkout -b week2

git push origin week2

git pull origin week2














